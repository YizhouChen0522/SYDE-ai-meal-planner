<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syde.mealplanner.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.syde.mealplanner.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="UserColumns">
        id, username, email, password, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.syde.mealplanner.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, email, password, status)
        VALUES (#{username}, #{email}, #{password}, #{status})
    </insert>

    <select id="selectById" resultMap="UserResultMap">
        SELECT
        <include refid="UserColumns"/>
        FROM `user`
        WHERE id = #{id}
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT
        <include refid="UserColumns"/>
        FROM `user`
        WHERE email = #{email}
    </select>

    <update id="updateStatusById">
        UPDATE `user`
        SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>
